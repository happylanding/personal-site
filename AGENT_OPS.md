# 智能体辅助运维方案

> 平台：CodeBuddy（无需额外注册/付费，已在用）
> 适用场景：文章发布、栏目调整、样式优化、视觉交互改进
> 开发仓库：`Galvin2026/personal-site`（CNB）→ 同步回 GitHub `happylanding/personal-site` → Cloudflare 自动部署

---

## 核心思路

```
你（写文章/提需求） → CodeBuddy（分析/生成/修改） → 你（审核确认，仅限大改动） → CodeBuddy（发布/提交）
```

> 小改动免评审直接合并部署，详见下方「协作规则」；仅大改动需先确认。

CodeBuddy 已经具备：读取项目文件、理解代码结构、编辑代码、执行 git 操作、运行构建——覆盖了网站运维的完整链路。你不需要学新工具，只需要**用对提示词**。

---

## 协作规则（2026-08-08 由 Galvin 确认，本仓库通用）

- **小改动直接合并部署**：对于代码量小、风险低、不改变功能语义的改动（文案/翻译修正、样式微调、Bug 修复、文档更新等），CodeBuddy 可以**自行合并到 main 并部署，无需再等人工评审**。
- **大改动仍需确认**：涉及结构性重构、数据库/内容 schema 变更、对外行为显著变化、或影响面较大的改动，仍需先提交 PR 并让 Galvin 审核确认后再合并。
- **每次合并前必须保证**：本地 `npm run build` 通过，且 PR CI 状态为 success，再执行合并。
- 合并后由 `.cnb.yml` 流水线自动同步到 GitHub → Cloudflare 自动部署，全程无需手动操作。

---

## 场景一：发布新文章（最重要）

### 流程

```
你写草稿 → 丢进 drafts/ → 说"处理草稿" → 智能体生成 Meta → 你确认 → 自动发布
```

### 第一步：写好文章，丢进草稿箱

把 `.md` 文件放到 `src/content/drafts/` 目录下。

**不需要写 frontmatter，不需要写日期，不需要写标签。** 最简单的格式：

```markdown
# 我的文章标题

正文第一段...

## 小节一
...

## 小节二
...
```

标题（`# 标题`）建议写上，这样智能体能更准确分析内容。

### 第二步：让智能体处理

在 CodeBuddy 中说一句话：

> 帮我处理草稿

智能体会自动：
1. 扫描 `src/content/drafts/` 下所有 `.md` 文件
2. 逐个分析内容，生成中英文 title/description/tags
3. 建议合适的 section（insights/ai/tips/books）
4. 展示完整的 frontmatter + 正文预览给你

### 第三步：审核确认

智能体会展示类似这样的结果：

```markdown
---
title: "xxx"
titleEn: "xxx"
description: "xxx"
descriptionEn: "xxx"
date: 2026-08-02
tags: ["标签1", "标签2", "标签3"]
tagsEn: ["tag1", "tag2", "tag3"]
section: tips
draft: false
---

（你的正文）
```

你审核后说：

> 确认，发布到 [section]

### 第四步：自动发布

智能体自动执行：草稿 → 写入 `articles/` → `astro build` 验证 → `git commit` → `git push` → Cloudflare 部署。

### 草稿箱位置

```
src/content/drafts/          ← 你丢草稿的地方
src/content/articles/        ← 智能体处理完放这里
```

草稿处理完后原始文件会保留在 `drafts/`，你可以手动删除或留着做备份。

---

## 场景二：调整网站框架

### 新增栏目

> 我想新增一个 [栏目名] 栏目，路由是 /[path]，功能类似现有的 [tips/books]

CodeBuddy 会：
1. 在 `config.ts` 的 section enum 中添加新值
2. 创建中英文列表页（`src/pages/[section].astro`、`src/pages/en/[section].astro`）
3. 更新导航栏（`Header.astro`）
4. 更新 `i18n/ui.ts` 双语字典
5. 构建验证 → 提交发布

### 删除/隐藏栏目

> 把 [栏目名] 栏目暂时隐藏，后续可能恢复

CodeBuddy 会：
1. 从导航栏移除入口
2. 保留路由文件和内容（不删数据）
3. 或设为 `draft` 模式，页面 404

---

## 场景三：样式与视觉优化

直接用自然语言描述效果即可：

| 需求 | 示例提示词 |
|------|-----------|
| 换配色 | "把网站主色调从黑白色改成暖色系，类似 Notion 风格" |
| 改字体 | "文章正文字体换成思源宋体" |
| 加动效 | "给文章卡片加个 hover 悬浮阴影效果" |
| 调布局 | "首页文章列表从双列改成单列排版" |
| 加组件 | "在每篇文章底部加一个相关文章推荐区域" |
| 改暗色模式 | "暗色模式下代码块的背景色太亮了，调暗一些" |

---

## 场景四：收藏夹收录（设计灵感库）

### 定位

「设计灵感收藏夹」是**内部参考体系**，不在导航/首页展示，仅供站主个人查阅。入口：

- 页脚的小书签图标（低调隐藏）
- 键盘快捷键：按 `g` 再按 `s` 直达 `/sites/`

### 收录流程

> 收录 https://xxx.com

CodeBuddy 会：
1. 采集网页内容，按四段式模板生成拆解报告（一句话定位 / 结构与信息架构 / 创意与设计拆解 / 亮点特色总结）
2. 在 `src/content/sites/` 新建 `slug.md`（带 `siteUrl` 字段）
3. 更新收藏夹索引表 `src/content/sites/sites-index.md`（注意：**不要**给索引文档加 `siteUrl`，否则会被误判成独立报告）
4. 构建验证 → 提交发布

### 数据位置

```
src/content/sites/sites-index.md   ← 收藏夹索引（入口文档，无 siteUrl）
src/content/sites/<slug>.md        ← 每份网站拆解报告（带 siteUrl）
```

页面路由（隐藏，sitemap 已过滤）：`/sites/`、`/sites/<slug>/`（含 `/en/` 前缀英文版）。

---

## 场景五：批量操作

| 需求 | 示例提示词 |
|------|-----------|
| 修改所有文章的某项 meta | "把所有 books 类文章的 featured 设为 false" |
| 全局样式替换 | "把所有页面的标题从 text-2xl 改成 text-3xl" |
| 添加新功能到所有页面 | "给所有页面底部加一个 Newsletter 订阅组件" |
| 迁移内容 | "把 tips 目录下所有标签包含 'CSS' 的文章移到 insights 栏目" |

---

## 进阶：设置自动化任务（CodeBuddy Automations）

如果有一些**周期性任务**，可以设置自动化：

| 任务 | 频率 | 说明 |
|------|------|------|
| 检查草稿箱 | 每天 | 扫描 `drafts/` 目录，提醒你有未发布的文章 |
| 网站健康检查 | 每周 | 运行 `astro build` 确认没有构建错误 |
| 依赖更新检查 | 每两周 | 检查 npm 依赖是否有安全更新 |

在 CodeBuddy 中说：

> 帮我创建一个自动化，每天检查 `src/content/` 目录下是否有新增或未发布的草稿文章，有的话提醒我

即可完成设置。

---

## 推荐的提示词模板（存下来直接用）

### 模板 A：发布新文章

```
帮我处理草稿
```

（更具体：）

```
帮我处理草稿，文章在 src/content/drafts/ 下，生成完整 frontmatter 后先给我预览。
```

### 模板 B：修改网站结构

```
我想对网站做以下调整：[描述调整内容]
请你：
1. 先分析需要修改哪些文件
2. 列出改动清单给我确认
3. 确认后再执行修改
```

### 模板 C：样式优化

```
我想调整网站视觉：[描述效果]
请你：
1. 先读取相关的 CSS/Astro 文件
2. 给出修改方案
3. 等我确认后执行
```

---

## 与你现在工作方式的关系

你已经这样用了。对比一下：

| 操作 | 之前 | 之后（标准化） |
|------|------|---------------|
| 发布文章 | 手动写 frontmatter → 手动推 | 粘贴正文 → AI 生成 meta → 审核 → 一键发布 |
| 改样式 | "安装 Typography 插件" | 一样，加一个"先展示方案再执行" |
| 改结构 | "隐藏联系方式" | 一样，加一个改动清单预览 |
| 加功能 | "加 bodyEn 字段" | 一样 |

本质不变，只是：
1. **先预览再执行**（防止改错）
2. **提示词模板化**（不用每次想怎么说）
3. **可选自动化**（定时检查草稿等）

---

## 总结

| 问题 | 答案 |
|------|------|
| 需要什么平台？ | 不需要新平台，CodeBuddy 就行 |
| 需要付费吗？ | 不需要 |
| 需要安装什么吗？ | 不需要，已经在了 |
| 最大的改变是什么？ | 用"先读、建议、确认、执行"四步法代替直接改 |
| 什么时候开始？ | 现在，下次写文章就直接试 |
