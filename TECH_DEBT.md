# Galvin V1 技术债台账

## 类型检查基线

Galvin 重构开始时，`astro check` 对 49 个既有页面报告 224 项错误，主要集中于冻结的英文阅读器、旧工具页内联脚本与未进入本次首个工作包的历史页面。现有生产构建能够通过，是因为部分静态路由在当前内容数据下不进入渲染路径；但这些错误不能被视为已解决。

因此，首笔基础提交采用双层质量策略：

1. `npm run check` 只校验本次新增的 Galvin 业务模块和测试，确保新代码从零开始保持严格类型安全。
2. `npm run check:astro` 保留全量 Astro 诊断，作为迁移过程中的技术债仪表盘；每次重构相关页面时，必须减少而非增加该错误基线。
3. 在生产合并前，所有仍公开可访问的页面必须通过全量 Astro 检查，或被有意删除/重定向并从检查范围移除。

## 已知历史范围

| 范围 | 处理策略 | 目标阶段 |
|---|---|---|
| `src/pages/en/**` | 冻结为迁移源，不新增英文内容；生产切换时永久迁移至中文 | P6 路由迁移 |
| `src/pages/books/**` | 旧公版书阅读器不再作为 Galvin 书架功能；替换为书目、笔记和合法外链 | P5 书架实现 |
| `src/pages/tools.astro` 内联脚本 | 造物工具重构时拆成有类型的客户端模块与测试 | P5 造物实现 |
| 旧通用文章页的路径/组件引用 | Galvin 文章模板重构时统一修正 | P4 内容模板 |
